from setuptools import setup

package_name = 'oak_test_pkg'

setup(
    name=package_name,
    version='0.0.1',
    packages=[],
    data_files=[],
    install_requires=['setuptools'],
    tests_require=['pytest'],
    entry_points={},
)
